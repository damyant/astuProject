package certificate

import examinationproject.Student

class AnnexerHStudents {
    /*Save Certificate requests (Summer Training) by Students(Save request and corresponding student)  */
    CertificateRequests certificateRequests
    Student student

    static constraints = {
    }
}
