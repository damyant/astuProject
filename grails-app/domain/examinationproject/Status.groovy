package examinationproject

class Status {
    /*save status(status for student registration,semester registration,certificate request, project etc)*/

    String status
    static constraints = {
    }
}
